openapi: 3.1.2
info:
  title: Composition API
  version: 1.0.0
paths:
  /oneof:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/SpecTestOneOfRequest'
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                oneOf:
                  - type: string
                  - type: string
components:
  schemas:
    SpecTestOneOfRequest:
      type: object
      properties:
        value: {}
